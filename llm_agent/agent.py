"""SOC-AI LLM triage agent entry point — implemented in P4."""

# TODO(P4): implement dual backend (Claude + Ollama), PII anonymiser, JSON validation.
raise NotImplementedError("llm_agent not yet implemented — run after P4")
